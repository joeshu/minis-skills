# SKILL_SCORING_STANDARD

评估时间基线：2026-04-15  
适用范围：`/var/minis/skills/` 下所有技能  
主要参考：`darwin-skill` 的 8 维评分框架

---

## 目标

把技能优化与评分从“凭感觉”变成“按统一维度评估”。

本规范用于：
- 新技能初评
- 技能优化后的复评
- 多个技能横向比较
- 冲高分 / 发布 / 封板前的统一收口

---

## 总原则

1. **先按技能真实职责设计，再按统一标准评分**
2. **评分口径统一，但不强制所有技能写成同一模板**
3. **结构评分 + 效果评分并用**
4. **能做 full test 时优先 full test；做不了时允许 dry-run，但必须标注**
5. **高分不代表无限扩写，重点是结构清晰、边界稳、实测有效**

---

## 标准评分 Rubric（8维，总分100）

### 结构维度（60分）

| # | 维度 | 权重 | 核心判断点 |
|---|---|---:|---|
| 1 | Frontmatter质量 | 8 | `name` 是否规范；`description` 是否说清“做什么 + 何时用 + 触发场景” |
| 2 | 工作流清晰度 | 15 | 是否有明确步骤、顺序、阶段；是否能直接执行 |
| 3 | 边界条件覆盖 | 10 | 是否说明何时不用、失败怎么办、异常如何 fallback |
| 4 | 检查点设计 | 7 | 关键决策前是否有检查点/确认点，避免失控推进 |
| 5 | 指令具体性 | 15 | 是否具体、少歧义；是否给出格式、清单、判断标准 |
| 6 | 资源整合度 | 5 | 是否正确引用 README / REPORT / scripts / test-prompts / execution-samples |

### 效果维度（40分）

| # | 维度 | 权重 | 核心判断点 |
|---|---|---:|---|
| 7 | 整体架构 | 15 | 结构是否完整协调；有无明显冗余、断层、职责混乱 |
| 8 | 实测表现 | 25 | 用测试 prompt 看输出是否明显优于 baseline |

---

## 评分公式

- 每个维度按 **1~10 分**评分
- **总分 = Σ(维度评分 × 权重) / 10**
- 满分 100

示例：
- 工作流清晰度 9.8 分，权重 15
- 加权得分 = 9.8 × 15 / 10 = 14.7

---

## 各维度评分说明

### 1. Frontmatter质量（8）
高分标准：
- `name` 清楚稳定
- `description` 说清用途、适用场景、触发语义
- 不空泛，不只写一句宽泛口号

扣分点：
- 描述只说“这是个很好用的技能”
- 没写何时触发
- 过长、过虚、不可判定

### 2. 工作流清晰度（15）
高分标准：
- 有阶段 / 步骤 / 路线
- 顺序清楚
- 执行动作与输出结果能对应上

扣分点：
- 大段原则，没有流程
- 只有理念，没有执行顺序
- 冲分、发布、维护等阶段混在一起

### 3. 边界条件覆盖（10）
高分标准：
- 说明何时不用
- 有 fallback
- 有误用约束
- 能识别相邻技能职责边界

扣分点：
- 只写 happy path
- 把所有情况都往自己技能里兜
- 没有失败处理与保守策略

### 4. 检查点设计（7）
高分标准：
- 在关键节点前有检查点
- 能阻止不成熟状态直接进入重流程
- 能区分继续优化 vs 进入维护

扣分点：
- 一路直冲没有判断
- 缺少阶段切换条件
- 没有最小可发布/可维护判断

### 5. 指令具体性（15）
高分标准：
- 输入、输出、判断规则具体
- 文件/目录/资产清单明确
- 模板可直接复用

扣分点：
- 大量“适当”“合理”“按需”但没有判断标准
- 缺少最小资产定义
- 缺少可执行模板

### 6. 资源整合度（5）
高分标准：
- `README.md`、`REPORT.md`、`test-prompts.json`、`execution-samples.md` 等资产衔接自然
- 路径和命名一致

扣分点：
- 文档与技能正文脱节
- 提到资源但没有对应文件
- 命名不统一

### 7. 整体架构（15）
高分标准：
- 结构层次自然
- 不堆砌重复段落
- 与整个技能生态分工清楚

扣分点：
- 同一件事重复三遍
- 章节顺序混乱
- 和相邻技能严重重叠

### 8. 实测表现（25）
高分标准：
- 用测试 prompt 跑时，with-skill 明显优于 baseline
- 输出更稳定、更贴任务目标
- 没有明显副作用（过度冗长、跑偏、重型化）

扣分点：
- 带 skill 和不带差不多
- skill 反而限制了输出
- 框架看起来很强，但真实任务中帮助不明显

---

## 评估模式

### A. full-test（优先）
适用：
- 有测试 prompt
- 能进行 with-skill vs baseline 对照

要求：
1. 选 2~8 个代表性 prompt
2. 对照 baseline 与 with-skill
3. 记录结果判断
4. 对维度 8 单独解释打分原因

### B. full-test style（人工对照）
适用：
- 当前环境不方便自动双跑
- 但可以人工按典型场景对照分析

要求：
- 明确写出 baseline 预期表现
- 明确写出 with-skill 预期表现
- 明确给出“明显优于 / 显著优于 / 基本持平”等结论

### C. dry-run（最低保底）
适用：
- 暂时没有时间做对照
- 只能按技能文本推演典型执行路径

要求：
- 必须明确标注 `dry-run`
- 不应把 dry-run 包装成 full-test

---

## 推荐评分流程

### Phase 1：读技能本体
- 读 `SKILL.md`
- 判断技能职责
- 找出相邻技能与边界

### Phase 2：读配套资产
优先检查：
1. `README.md`
2. `test-prompts.json`
3. `execution-samples.md`
4. `REPORT.md`

### Phase 3：做 1~7 维结构评分
- 每维给分
- 每维给一句短理由

### Phase 4：做维度 8 效果评分
- 优先 full-test
- 否则 full-test style
- 再不行 dry-run

### Phase 5：汇总总分
- 输出维度表
- 输出总分
- 输出主要优点 / 主要短板 / 下一步建议

---

## 推荐分段解释

### 95~100
- 接近生产级 / 生产级
- 核心结构稳定
- 边界与测试较成熟
- 后续以回归、样本、边界修正为主

### 90~94.9
- 很优秀
- 已具备稳定可用性
- 仍有若干边界或样本层可加强

### 85~89.9
- 较强
- 已经成型
- 但在边界、检查点、报告或实测支撑上还有明显提升空间

### 80~84.9
- 可用
- 有明确结构
- 但还不够稳定，不建议直接视为成熟版本

### 79.9 以下
- 需要继续重构
- 可能存在结构缺陷、职责模糊或实测支撑不足

---

## 推荐报告模板

```md
# <skill-name> Full Test 评分报告

评估时间：YYYY-MM-DD
评估模式：full-test / full-test style / dry-run

## 一、测试样例覆盖
- Case 1
- Case 2
- Case 3

## 二、with-skill vs baseline 对照
### Case 1
**Prompt**：...
**Baseline**：...
**With Skill**：...
**结果判定**：...

## 三、实测表现评分（维度 8）
**维度 8 = x.x / 10**

## 四、按达尔文 8 维汇总
| # | 维度 | 权重 | 评分 | 加权得分 |
|---|---|---:|---:|---:|
...

**总分 = xx.xx / 100**

## 五、结论
### **xx.x / 100**
- 优势：...
- 短板：...
- 下一步：...
```

---

## 使用规则

- 以后所有技能评分，默认优先参考本标准
- 如果某个技能有专门评分逻辑，可以在本标准基础上加专项说明
- 如果报告用了 `full-test style` 或 `dry-run`，必须显式写出，不得混淆为自动实测
- 评分不是目的，**指导下一步优化动作**才是目的

---

## 与 darwin-skill 的关系

本文件是对 `darwin-skill` 评分框架的**仓库级固化与通用化**。

定位区别：
- `darwin-skill`：技能优化器 / 评分器 / 方法论来源
- `SKILL_SCORING_STANDARD.md`：全仓库统一评分口径

因此：
- 优化时可以参考 `darwin-skill`
- 评分时默认优先使用本标准
