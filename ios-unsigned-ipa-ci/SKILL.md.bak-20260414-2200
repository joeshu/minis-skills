# make-ipa

**技能名称**: make-ipa  
**目标**: 帮助用户把 iOS（SwiftUI / XcodeGen）项目完整地上传到 GitHub，自动化生成 **未签名 device‑only IPA** 并通过 GitHub Actions 持续交付。

## 触发词
- iOS应用构建、IPA生成、iOS CI
- build iOS app、generate IPA、iOS CI
- XcodeGen项目、SwiftUI应用、未签名IPA
- XcodeGen project、SwiftUI app、unsigned IPA

## 适用场景
- 新iOS项目需要CI/CD流水线
- 现有iOS项目需要优化代码和重新构建
- 检查GitHub Actions构建状态
- 下载和验证构建产物

> **核心理念**：一次成功的 CI 打通后，后续迭代只改代码，除非出现明确的 CI/Workflow 失效或用户主动要求修改。  

---

## 1️⃣ 双模式入口（模式 A / 模式 B）

| 模式 | 何时进入 | 主要操作 |
|------|----------|----------|
| **模式 A – 首轮打通** | <ul><li>仓库不存在或空仓库</li><li>工作流文件 `.github/workflows/build-unsigned-ipa.yml` 不存在</li><li>从未成功生成过 IPA（无相应 artifact）</li></ul> | 1. 根据用户需求生成 **功能代码**（SwiftUI + XcodeGen）<br>2. 在本地创建 **Xcode 项目结构**（`project.yml`、源文件、`ExportOptions.plist`）<br>3. 生成 **GitHub Actions workflow**（`build-unsigned-ipa.yml`）<br>4. 初始化 Git 仓库 → **push** 代码+workflow<br>5. 触发一次 **CI**，确认能够产出符合要求的 IPA |
| **模式 B – 后续迭代** | <ul><li>仓库已存在且已包含 `build-unsigned-ipa.yml`</li><li>最近一次 `Build unsigned IPA` 运行成功（artifact 已生成）</li><li>项目根目录已有 `project.yml` / `.xcodeproj`</li></ul> | 1. **仅优化代码**（bug、可验证性、交互等）<br>2. `git add/commit/push` → 让已有 workflow 自动跑<br>3. **仅在 CI 失败或用户请求** 时才回头修改 workflow 或工程结构 |

> **实现提示**：在每次运行前通过 `git ls-remote`、`git rev-parse --is-inside-work-tree`、以及 GitHub Actions API 查询最近一次 workflow 运行状态来判定当前模式。  

---

## 2️⃣ 已打通判定条件（“流程已打通”）

任意满足下列 **一条** 即认为流程已打通，自动进入 **模式 B**：

1. **Workflow 文件**：仓库根目录下存在 `.github/workflows/build-unsigned-ipa.yml`（或自定义名称）
2. **最近一次 CI 成功**：通过 GitHub API 检查最近一次 `Build unsigned IPA` **conclusion = success**
3. **Artifact 命名符合**：最新 artifact 名称匹配 `$(PROJECT_NAME).ipa`（或用户在 `ci-config.yml` 中自定义的 `artifact_name`）
4. **工程结构完整**：`project.yml` 与对应的 `.xcodeproj`（或 `*.xcworkspace`）均存在

> **在代码中**：用 `file_exists` / `git log -1 --format=%B` 判断，或直接在 CI 里利用 `actions/download-artifact` 检查。  

---

## 3️⃣ 默认不动 Workflow 的原则（后续迭代）

除非出现以下 **任意** 情况，**不对 workflow 文件做任何改动**：

- 新增/升级 **构建依赖**（例如引入 Swift Package、CocoaPods）导致编译方式变化
- 需要 **更改产物名称**（用户在 `ci-config.yml` 中修改 `artifact_name`）
- **CI 运行失败**（结论为 `failure`、`cancelled`、`timed_out`）
- 用户 **明确要求** 调整 workflow（如增加缓存、改为手动触发等）

这样可以保证已经“打通”的流水线保持 **稳定**，避免因为代码小改动而意外破坏 CI。  

---

## 4️⃣ 改代码优先级模板（代码迭代指引）

当用户下达 “继续优化代码” 或类似指令时，按照下面的 **优先级顺序** 自动挑选要做的改动，保证每次提交聚焦且价值最高：

1. **修复 Bug**（编译错误、运行时异常）
2. **提升可验证性**（加入单元测试、断言、错误日志）
3. **提升交互可用性**（按钮状态、可访问性、错误提示）
4. **清理重复逻辑**（抽取公共函数、合并同类代码）
5. **结构重构**（模块划分、依赖注入、目录规范）
6. **样式美化**（代码排版、SwiftFormat、注释完善）

> **实现方式**：在 `generate_patch.py` 中预定义这几类模板；用户未指明细节时，自动选择第一个满足条件的模板。  

---

## 5️⃣ 提交粒度规则（Commit 规范）

- **一次只做一类改动**（业务逻辑 / UI / CI）
- **Commit Message** 采用 **Conventional Commits** 规范：
  - `fix:` bug 修复
  - `feat:` 新功能 / 业务代码改动
  - `refactor:` 重构（不影响外部行为）
  - `style:` 代码格式、注释、空格等
  - `ci:` CI/Workflow 相关改动（仅在模式 A 或 CI 失败时）
- **小步提交**：如有多处 bug，分别提交，每次 `git push` 触发独立 CI 运行，便于定位回滚点。

---

## 6️⃣ 失败后处理分支

| 失败类型 | 首要检查项 | 处理流程 |
|----------|------------|----------|
| **Push 失败** | <ul><li>SSH/HTTPS remote URL 是否正确</li><li>`GITHUB_TOKEN`（或 SSH key）是否已配置且有 write 权限</li><li>网络连通性（`curl -I https://github.com`）</li></ul> | 1. 给出具体错误信息（例如 “permission denied”）<br>2. 提示用户检查/补全环境变量或 remote URL<br>3. **不触发 CI**，等待用户重新 push |
| **CI 运行失败** | <ul><li>代码编译错误</li><li>工程文件缺失或不匹配（`project.yml` 与 `.xcodeproj`）</li><li>Workflow 语法或权限错误</li></ul> | 1. 下载 `build.log` 并返回给用户（`minis://attachments/build.log`）<br>2. 自动进入 **模式 B** 的 “改代码优先级模板”，尝试在代码层面修复<br>3. 若仍无法解决，则 **进入模式 A** 的 “改 Workflow/工程” 分支，自动生成或修复 workflow |
| **CI 成功但功能不可用** | <ul><li>运行时 UI/交互问题</li><li>缺少必要的调试信息 / fallback</li></ul> | 1. 直接进入 **代码优化**（步骤 4）<br>2. 在 commit message 中加入 `feat:` 或 `fix:`，并在 PR 中标记 “功能改进”。 |

---

## 7️⃣ 产物稳定性约束

- **产物名称**（`artifact_name`）一旦在第一次成功运行后被用户接受，**后续不再自动更改**（除非用户在 `ci-config.yml` 中手动修改）。
- **Workflow 文件名** 固定为 `build-unsigned-ipa.yml`（或用户在 `ci-config.yml` 中配置的 `workflow_name`），后续仅在 **CI 失败且需要改动** 时才更新。
- **Release / Tag**：默认采用 **`v<date>`**（如 `v20260408`）并以 **Draft** 方式创建，只有用户明确要求才升级到正式 Release。
- **IPA 文件名**：始终保持 `<PROJECT_NAME>.ipa`，不随 commit SHA 变化，方便用户固定路径下载。

---

## 8️⃣ 迭代输出格式（统一报告）

### 8.1 首轮打通（模式 A）
```
✅ **首轮打通完成**
**仓库**: https://github.com/<owner>/<repo>
**CI Run ID**: 123456789 (GitHub Actions)
**Workflow**: .github/workflows/build-unsigned-ipa.yml
**Artifact**: minis://attachments/<PROJECT_NAME>.ipa
**状态**: 成功 🎉

> 关键步骤
- 生成功能代码（<文件列表>）
- 生成 XcodeGen 项目 (`project.yml`)
- 生成 workflow (`build-unsigned-ipa.yml`)
- 初始化 Git、首次 push
- CI 完整跑通，产出 IPA

如需后续迭代，只需提交代码，系统会自动触发 CI。
```

### 8.2 后续迭代（模式 B）
```
🔄 **后续迭代报告**
**Commit**: abcdef1 (feat: improve quick test button interaction)
**CI Run ID**: 987654321
**状态**: 成功 🎉

**本次改动**
- 修复了快速测试按钮的交互卡顿（代码文件 A.swift, B.swift）
- 添加了 `assert` 检查，以提升可验证性

**后续建议**
- 若需要更改产物名称或新增依赖，请在 `ci-config.yml` 中修改后重新提交。
- 当前 workflow 与产物名称已固定，如无 CI 失败请保持不变。

✅ 如本次 CI 已成功，您可以直接下载最新 IPA：
[minis://attachments/<PROJECT_NAME>.ipa](minis://attachments/<PROJECT_NAME>.ipa)

⚠️ 如 CI 失败，请查看 `minis://attachments/build.log` 并根据提示修复代码或 workflow。
```

> **输出方式**：技能执行完毕后 **自动发送上述 Markdown**（根据模式自动选择）并在需要时附带 `build.log`、`checksum.txt` 等附件链接。  

---

## 实现要点（代码层面提示）

| 文件 | 关键实现 |
|------|----------|
| `generate_project.py` | 根据 `ci-config.yml` 渲染 Jinja2 模板，生成 `project.yml`、Swift 代码，返回文件路径列表 |
| `generate_workflow.py` | 渲染 `build-unsigned-ipa.yml`（Jinja2），支持 `workflow_name` 参数 |
| `ci_manager.sh` | 1️⃣ 判定模式（A/B）<br>2️⃣ 根据模式调用相应的生成/提交脚本<br>3️⃣ 使用 `git` 检查 remote、branch、status<br>4️⃣ 调用 GitHub API（`curl -H "Authorization: token $GITHUB_TOKEN"`）获取最近一次 workflow run 状态 |
| `post_process.py` | 读取 CI 运行结果（`jobs[].conclusion`），生成 **8.1 / 8.2** 报告 Markdown 并写入 `/var/minis/attachments/report.md`（随后返回 `minis://attachments/report.md`） |
| `failure_handler.sh` | 根据 `git push` 或 `workflow run` 的错误码分支处理，输出明确的诊断信息（SSH、Token、网络、代码） |

> **工具依赖（Minis Alpine）**  
`apk add git curl jq python3 py3-pip bash`  
`pip install jinja2`

---

## 9️⃣ GitHub API 查询 Actions 状态 & 下载 IPA（新增标准流程）

> 适用场景：用户说“检查 action”“查询构建状态”“下载 ipa”。

### 9.1 前置条件
- 必须有 `GITHUB_TOKEN` 环境变量（具备 repo 读取权限；私有仓库需对应访问权限）。
- 禁止输出 token 明文；仅在命令中引用 `$GITHUB_TOKEN`。

### 9.2 查询最近运行状态（推荐 API）
优先用 REST API 获取最近 5 条运行记录并结构化输出：

```sh
curl -sSL \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/<owner>/<repo>/actions/runs?per_page=5"
```

建议解析字段：
- `run_number`
- `id`
- `status`（`queued`/`in_progress`/`completed`）
- `conclusion`（`success`/`failure`/`cancelled`/`timed_out`）
- `head_sha`
- `html_url`

当最新 run 为 `in_progress` 时，轮询 `GET /actions/runs/{run_id}`，直到 `status=completed`。

### 9.3 下载失败日志用于定位
当 `conclusion=failure`：

1. 下载日志 zip：
```sh
curl -sSL \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/<owner>/<repo>/actions/runs/<run_id>/logs" \
  -o run_logs.zip
```
2. 解压后 grep 错误：
```sh
unzip -q run_logs.zip -d run_logs
grep -Rin "error:\|BUILD FAILED\|warning:" run_logs
```
3. 把关键错误行反馈给用户，并给出修复提交。

### 9.4 下载成功构建的 IPA（artifact）
当 `conclusion=success`：

1. 查询 artifacts：
```sh
curl -sSL \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  "https://api.github.com/repos/<owner>/<repo>/actions/runs/<run_id>/artifacts"
```
2. 取目标 artifact 的 `archive_download_url`（通常是 `*-ipa`）。
3. 下载并解压：
```sh
curl -sSL \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  "<archive_download_url>" -o artifact.zip
unzip -o -q artifact.zip
```
4. 将 `.ipa` 复制到 `/var/minis/attachments/`，在回复中给出可点击链接：
`[下载 xxx.ipa](minis://attachments/xxx.ipa)`

### 9.5 输出规范（状态 + 下载）
技能输出应包含：
- 最新 run 编号、提交短 SHA、状态/结论
- 若失败：关键报错 + 已执行修复（如有）
- 若成功：artifact 名称、大小、下载链接（minis://attachments/...）


### 9.7 技能内置脚本（推荐优先）
技能已内置可复用脚本：
- `/var/minis/skills/ios-unsigned-ipa-ci/references/gha_ipa_fetch.sh`

执行方式：
```sh
sh /var/minis/skills/ios-unsigned-ipa-ci/references/gha_ipa_fetch.sh <owner> <repo> [run_id]
```

示例：
```sh
sh /var/minis/skills/ios-unsigned-ipa-ci/references/gha_ipa_fetch.sh joeshu minesweeper-ipa-0408-151615
```

脚本行为约定：
- 自动打印最新 run 状态（run number/id/status/conclusion/sha/url）
- 若未传 `run_id`：自动选择“最新成功” run
- 自动下载 artifact 并解压 `.ipa`
- 自动复制到 `/var/minis/attachments/`
- 输出：`Minis URL: minis://attachments/<ipa文件名>`

在技能执行中，当用户请求“检查 action 并下载 ipa”时，优先调用该脚本，而不是重复拼接临时 curl 命令。

