<!-- 2026-04-14 23:55:00 -->
## opencode2api-enhanced 项目总备忘录
- 正式工作目录：`/var/minis/workspace/opencode2api-enhanced`；旧目录 `/var/minis/workspace/opencode2api` 仅保留为历史/双 remote 工作副本。
- 操作偏好：修改前先备份；改动拆成最小、可回滚的独立提交；避免把无关改动混入同一 commit。
- 当前主验证方式：优先使用本地 `npm run verify:smoke`；当前环境下 Jest 不稳定，不作为主验证手段；GitHub `Smoke Check` 与 `Publish Docker Image` 当前均稳定通过。
- 模块化重构进展：已稳定拆出 `errors.js`、`backend-health.js`、`backend-runtime.js`、`request-runtime.js`、`timeouts.js`、`models.js`、`image.js`、`prompt-utils.js`、`cleanup.js`、`tool-overrides.js`、`events.js`、`opencode-path.js`、`message-orchestration.js`、`prompt-executor.js`、`conversation-cleanup.js`、`start-proxy-config.js`、`server-runtime.js`、`response-builders.js`、`stream-builders.js`、`responses-stream-builders.js`、`session-preparation.js`、`user-facing-errors.js`、`output-sanitizer.js`、`session-cleanup.js`、`latency-tracker.js`、`active-model-cache.js`，并持续按“每刀 smoke 通过后再提交/推送”的方式推进。
- 关键经验：`verify:smoke` 脚本迁移目录后需同步修正相对导入路径（如 `../src/proxy.js`）；当前 `@opencode-ai/sdk` 在本环境下的 session 行为需按实际探针结果对齐，`session.create()` 可能访问 `GET /session`，`session.prompt()`/`session.messages()` 可能访问 `GET /session/{id}/message`；编写 smoke mock backend 时应以探针结果为准，不按 REST 命名直觉猜测；本地 iSH 中 Node 运行时偶发不稳定，主线是否健康应以 GitHub Smoke Check 为更高优先级基线。
- 维护阶段新增重点：围绕手机端体验已额外落地 `user-facing-errors.js`（更友好错误提示）、`output-sanitizer.js`（异常污染输出过滤）、`session-cleanup.js`（延迟 session 清理）、`latency-tracker.js`（首包时间埋点）、`active-model-cache.js`（跳过重复 activeModel update），同时支持 `OPENCODE_PROFILE=stable/tools-stable` 模式切换，并补充了移动端 SSE / 反代部署文档。
- 当前结构判断：`src/proxy.js` 中多数工具层与运行时辅助层已拆出，项目已进入“稳定维护 + 按需重构”阶段。
- 后续路线：若继续重构，优先考虑 chat/responses 编排辅助逻辑，再评估是否需要拆 routes 分文件；如无明显收益，可暂停大拆分，转入功能开发与维护。
- 记忆整理约定：后续与 opencode2api-enhanced 相关的长期信息，优先更新这条总备忘录，不再新增零散同主题记忆。


